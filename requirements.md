# 習慣トラッカー MVP 要件定義書

## プロジェクト概要

| 項目 | 内容 |
|------|------|
| プロダクト名 | （未定） |
| コンセプト | 毎日の習慣タスクをタップで完了にできるシンプルな習慣トラッカー |
| ターゲットユーザー | 自分（REN） |
| プラットフォーム | PWA（Progressive Web App） |
| 技術スタック | 素の HTML / CSS / JavaScript（フレームワークなし） |
| データ保存 | localStorage（ブラウザローカル保存） |
| デプロイ先 | GitHub Pages |
| カラーテーマ | ダークモード（黒背景） |

## MVP 機能一覧

### 1. タスク一覧表示（メイン画面）
* 登録済みの習慣タスクが TODO リスト風 に縦に並ぶ
* 各タスクには完了/未完了の状態がある
* 未完了タスクと完了済みタスクが視覚的に区別できる（色の変化）
* タスクは登録したら 基本固定（頻繁な追加・削除は想定しない）
* 画面上部に今月のサマリー を表示: タスクごとの達成日数（例: 「英語動画 18/28日」）
* 月の経過日数を分母とする（例: 2月12日時点なら「/12」）

### 2. タップで完了 + メモ入力
* タスクをタップすると 「完了」状態 になる
* 完了時、タスクの下に インラインでメモ入力欄が展開 される
* メモは 書いても書かなくてもOK（任意）
* 完了のフィードバックは シンプル（色が変わる程度）

### 3. 毎朝 3:00 自動リセット
* 毎日 午前 3:00 に全タスクが未完了状態にリセットされる
* 実装方式: アプリを開いたときに最終リセット日時をチェックし、3:00 を過ぎていたらリセットする（バックグラウンド処理は不要）
* リセット前の完了状態・メモは 過去ログとして保存 される

### 4. タスク管理（設定画面）
* タスクの 追加 ができる
* タスクの 削除 ができる
* タスクの 並び替え ができる
* 基本的に初回セットアップ後はあまり触らない想定

### 5. 過去ログ閲覧
* 全期間 の履歴を閲覧できる（保存期間の上限なし）
* 月ごとに区切り を入れて表示（月ヘッダー + タスクごとの月間達成サマリー）
* 各日付ごとに、どのタスクを完了したか・メモ内容が確認できる
* 「今日のタスク」タブと「過去ログ」タブを タブ切り替え で表示

### 6. データエクスポート（JSON）
* 設定画面から 全データを JSON ファイルとしてダウンロード できる
* エクスポート対象: タスク一覧 + 全過去ログ
* ファイル名: habit-tracker-YYYY-MM-DD.json
* ブラウザのデータが消えた場合のバックアップ用途

## 画面構成

### タブ構成（1 画面 + タブ切り替え）

```
┌─────────────────────────────┐
│  [今日]  [ログ]             │  ← タブ
├─────────────────────────────┤
│  2月のサマリー              │
│  英語動画    8/12日         │
│  高速音読   10/12日         │
│  数学ガール  6/12日         │
│  筋トレ     11/12日         │
├─────────────────────────────┤
│                             │
│  □ 英語動画                 │
│  ■ 高速音読 ✓              │
│    メモ: 10分やった          │  ← インライン展開
│  □ 数学ガール               │
│  □ 筋トレ                   │
│                             │
│            今日 2/4 完了     │
│                             │
│  ⚙ 設定                    │
└─────────────────────────────┘
```

### ログタブ

```
┌─────────────────────────────┐
│  [今日]  [ログ]             │
├─────────────────────────────┤
│                             │
│  ── 2月 ──────────────────  │  ← 月の区切り
│  英語動画 8/12 | 高速音読   │
│  10/12 | 数学ガール 6/12 |  │
│  筋トレ 11/12               │  ← 月間サマリー
│                             │
│  2/12（水）  3/4 完了       │
│    ✓ 英語動画               │
│    ✓ 高速音読 - 15分        │
│    ✓ 筋トレ - スクワット    │
│    ✗ 数学ガール             │
│                             │
│  2/11（火）  4/4 完了 🎉    │
│    ✓ 英語動画 - TED見た     │
│    ✓ 高速音読               │
│    ✓ 数学ガール - p.45まで  │
│    ✓ 筋トレ                 │
│                             │
│  ...                        │
│                             │
│  ── 1月 ──────────────────  │  ← 前月の区切り
│  英語動画 20/31 | ...       │
│                             │
│  ...（全期間スクロール）     │
└─────────────────────────────┘
```

## データ設計

### localStorage キー構成

| キー | 内容 | 型 |
|------|------|-----|
| tasks | 登録タスク一覧 | Task[] |
| log-YYYY-MM-DD | 各日の完了ログ | DayLog |
| lastResetDate | 最終リセット日 | string (YYYY-MM-DD) |

### データ型

```javascript
Task {
  id: string          // ユニークID
  name: string        // タスク名（例: "英語動画"）
  order: number       // 並び順
  done: boolean       // 今日の完了状態
  memo: string        // 今日のメモ（任意）
}

DayLog {
  date: string        // "YYYY-MM-DD"
  entries: DayLogEntry[]
}

DayLogEntry {
  taskId: string
  taskName: string    // タスク名（削除後も表示できるよう保存）
  done: boolean
  memo: string
}
```

## 技術仕様

### ファイル構成

```
habit-tracker/
├── index.html        // メインHTML
├── style.css         // スタイル（ダークモード）
├── app.js            // アプリロジック
├── manifest.json     // PWA マニフェスト
├── sw.js             // Service Worker
└── icons/
    ├── icon-192.png
    └── icon-512.png
```

### PWA 要件
* manifest.json でホーム画面追加対応
* sw.js でオフラインキャッシュ（静的ファイルのみ）
* アイコン: 192x192, 512x512

### リセットロジック（擬似コード）

```
アプリ起動時:
  現在時刻を取得
  lastResetDate を取得

  if 今日の日付（3:00基準）> lastResetDate:
    今日のタスク状態をログに保存
    全タスクの done を false に、memo を空にリセット
    lastResetDate を更新

  ※「今日の日付（3:00基準）」= 現在時刻が 0:00-2:59 なら前日、3:00以降なら当日
```

### デプロイ
* GitHub リポジトリにプッシュ → GitHub Pages で公開
* https://<username>.github.io/habit-tracker/ でアクセス

## 初期登録タスク
1. 英語動画
2. 高速音読
3. 数学ガール
4. 筋トレ

## MVP に含めないもの（将来の拡張候補）
* サーバー/DB 連携（マルチデバイス同期）
* プッシュ通知・リマインダー
* ストリーク（連続日数）表示
* 統計・グラフ表示
* ライトモード対応
* 曜日ごとのタスク切り替え
* タスクのカテゴリ分け

## Claude Code 向け実装指示

### 概要
この要件定義書に基づいて、習慣トラッカー PWA を実装してください。

### 技術方針
* フレームワーク不使用: 素の HTML / CSS / JavaScript のみ
* ビルドツール不使用: バンドラーやトランスパイラは使わない
* 1リポジトリ: GitHub Pages でそのまま公開できる構成
* モバイルファースト: スマホでの操作を最優先に設計

### 実装の優先順位
1. タスク表示 + タップ完了 + メモ入力（コア体験）
2. localStorage への保存・読み込み
3. 3:00 リセットロジック
4. 過去ログ閲覧（タブ切り替え）
5. タスク管理（追加・削除・並び替え）
6. JSON エクスポート
7. PWA 化（manifest.json + Service Worker）

### デザイン指示
* ダークモード（黒背景 + 明るいテキスト）
* TODO リスト風のシンプルな UI
* 完了タスクは色の変化でフィードバック（派手な演出は不要）
* メモ入力はタスクの下にインライン展開
* タブは「今日」「ログ」の 2 つ
* 設定はギアアイコン等でアクセス

### ファイル構成
```
habit-tracker/
├── index.html
├── style.css
├── app.js
├── manifest.json
├── sw.js
└── icons/
    ├── icon-192.png
    └── icon-512.png
```

### デプロイ
```bash
git init
git add .
git commit -m "Initial commit: habit tracker MVP"
git remote add origin https://github.com/<username>/habit-tracker.git
git push -u origin main
# GitHub Pages を main ブランチの / (root) から公開
```
